import React from 'react';

const Card = () => {  

  // const textStyle = {
  //   backgroundColor: theme === "light" ? "white" : "black", 
  //   color: theme === "light" ? "black" : "white",
  // };

  return (
    <div>
    <h1>Hello World</h1>
    </div>
  );
};

export default Card;
